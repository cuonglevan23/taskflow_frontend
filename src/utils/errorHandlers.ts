import { AxiosError } from 'axios';

/**
 * Safe error logger for Axios errors - Silent version
 * Type-safe way to handle Axios errors without console output
 */
export function logAxiosError(error: unknown): void {
  // Silent error handling - logs removed per user request
  // Error details are handled by the calling functions
}
